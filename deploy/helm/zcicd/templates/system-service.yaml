apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "zcicd.fullname" . }}-system-service
  labels:
    {{- include "zcicd.labels" . | nindent 4 }}
    app.kubernetes.io/name: system-service
spec:
  replicas: {{ .Values.systemService.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: system-service
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: system-service
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: system-service
          image: "{{ .Values.global.imageRegistry }}{{ .Values.systemService.image.repository }}:{{ .Values.systemService.image.tag }}"
          imagePullPolicy: {{ .Values.systemService.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          volumeMounts:
            - name: config
              mountPath: /configs/config.yaml
              subPath: config.yaml
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.systemService.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "zcicd.fullname" . }}-config
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "zcicd.fullname" . }}-system-service
  labels:
    {{- include "zcicd.labels" . | nindent 4 }}
    app.kubernetes.io/name: system-service
spec:
  type: {{ .Values.systemService.service.type }}
  ports:
    - port: {{ .Values.systemService.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: system-service
    app.kubernetes.io/instance: {{ .Release.Name }}
