apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "zcicd.fullname" . }}-deploy-service
  labels:
    {{- include "zcicd.labels" . | nindent 4 }}
    app.kubernetes.io/name: deploy-service
spec:
  replicas: {{ .Values.deployService.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: deploy-service
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: deploy-service
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: deploy-service
          image: "{{ .Values.global.imageRegistry }}{{ .Values.deployService.image.repository }}:{{ .Values.deployService.image.tag }}"
          imagePullPolicy: {{ .Values.deployService.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.deployService.service.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "zcicd.fullname" . }}-config
            - secretRef:
                name: {{ include "zcicd.fullname" . }}-secret
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.deployService.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "zcicd.fullname" . }}-deploy-service
  labels:
    {{- include "zcicd.labels" . | nindent 4 }}
    app.kubernetes.io/name: deploy-service
spec:
  type: {{ .Values.deployService.service.type }}
  ports:
    - port: {{ .Values.deployService.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: deploy-service
    app.kubernetes.io/instance: {{ .Release.Name }}
